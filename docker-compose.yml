services:
  myward-limpopo:
    build:
      context: .
      args:
        VITE_APP_BASE_URL: ${VITE_APP_BASE_URL:-https://limpopo.myward.khulanode.com}
        VITE_GOOGLE_ADSENSE_CLIENT_ID: ${VITE_GOOGLE_ADSENSE_CLIENT_ID:-}
    environment:
      VITE_APP_BASE_URL: ${VITE_APP_BASE_URL:-https://limpopo.myward.khulanode.com}
      VITE_GOOGLE_ADSENSE_CLIENT_ID: ${VITE_GOOGLE_ADSENSE_CLIENT_ID:-}
    ports:
      - "8080:80"
